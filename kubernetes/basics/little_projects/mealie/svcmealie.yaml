apiVersion: v1
kind: Service
metadata:
  name: mealie-loadbalancer
  namespace: mealie
  labels:
    app: mealie
spec:
  ports:
    - port: 9000
      protocol: TCP
      targetPort: 9000
  selector:
    app: mealie
  sessionAffinity: None
  type: LoadBalancer

